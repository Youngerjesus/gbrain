# Invariant And Validator Matrix

## Required output fields

- Invariant:
- Protected state fields:
- Validator name:
- Validator input:
- Validator output:
- Failure code:
- Repair/retry/replan eligibility:

## Reject when

- The validator checks only JSON shape while the invariant is semantic.
- The invariant has no deterministic validator.
- The repair path can mutate product-owned state without an explicit authority rule.
